import { CompHostDirective } from './comp-host.directive';

describe('CompHostDirective', () => {
  it('should create an instance', () => {
    const directive = new CompHostDirective();
    expect(directive).toBeTruthy();
  });
});
